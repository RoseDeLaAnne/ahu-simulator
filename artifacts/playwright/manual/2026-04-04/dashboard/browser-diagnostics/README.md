# Browser diagnostics evidence

This folder keeps the minimal tracked browser/WebGL evidence required by
`/readiness/demo` after a clean clone.

The full manual Playwright screenshot set is local runtime evidence and remains
ignored by default.
