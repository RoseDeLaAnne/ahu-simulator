# Shared Package

Use this package only for code reused by multiple independent features.

Do not place feature-specific logic here.
Prefer keeping code inside a feature until real cross-feature reuse appears.
