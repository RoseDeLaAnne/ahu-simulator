# TODO

## Принятые рабочие решения для текущей версии

- Учебно-обобщенная ПВУ используется как базовая модель до получения внешних уточнений.
- Первая версия реализуется как стационарный интерактивный расчет с упрощенной динамикой помещения.
- PID-регулятор и расширенный экспорт отнесены за пределы текущего MVP.
- В проекте используются только синтетические или обезличенные данные.
- Рабочий baseline по `P0` зафиксирован в `config/p0_baseline.yaml` и `docs/19_p0_baseline.md`.

## P0. Обязательно до старта кодирования

- [x] Подтвердить, моделируется ли конкретная установка предприятия или учебно-обобщенная схема.
- [x] Уточнить перечень обязательных входных параметров.
- [x] Уточнить перечень обязательных выходных показателей.
- [x] Подтвердить, нужен ли динамический режим в первой версии.
- [x] Подтвердить, нужен ли PID-регулятор в первой версии.
- [x] Согласовать минимальный набор сценариев для защиты.
- [x] Согласовать формат валидации модели.
- [x] Зафиксировать правило: использовать только синтетические или обезличенные данные.

## P0. Ядро модели

- [x] Описать сущность `SimulationParameters`.
- [x] Описать сущность `SimulationState`.
- [x] Описать сущность `SimulationResult`.
- [x] Описать логику расчета температуры приточного воздуха.
- [x] Описать логику влияния фильтра на режим.
- [x] Описать логику влияния расхода воздуха.
- [x] Описать правила формирования предупреждений.
- [x] Подготовить 3-5 эталонных режимов для тестов.

## P0. API

- [x] Спроектировать endpoint `POST /simulation/run`.
- [x] Спроектировать endpoint `GET /scenarios`.
- [x] Спроектировать endpoint `GET /trends`.
- [x] Определить Pydantic-ограничения диапазонов.
- [x] Подготовить smoke-test для API.

## P0. UI

- [x] Определить состав главного экрана.
- [x] Нарисовать схему расположения блоков интерфейса.
- [x] Зафиксировать набор контролов на панели параметров.
- [x] Определить минимум графиков для первой версии.
- [x] Зафиксировать визуальные статусы: норма, риск, авария.
- [x] Спроектировать состав полноценной 2D SVG-модели установки.
- [x] Зафиксировать список `visual_id` для узлов, датчиков и потоков.
- [x] Разделить view-model визуализации и layout Dash.
- [x] Подготовить правила привязки цветов, подписей и тревог к 2D-узлам.
- [x] Определить, какие обновления 2D-сцены должны идти через clientside callbacks.

## P1. Качество

- [x] Подготовить unit-тесты расчетов.
- [x] Подготовить integration-тесты API.
- [x] Подготовить сценарный тест «зима».
- [x] Подготовить сценарный тест «загрязнение фильтра».
- [x] Подготовить проверку граничных параметров.
- [x] Вывести validation-matrix в API для повторяемой ручной проверки.
- [x] Вывести таблицу «модель -> эталон» в dashboard как `Validation Pack`.
- [x] Вывести реестр внешних методических оснований в dashboard и API как `Validation Basis`.
- [x] Вывести таблицу «модель -> ручной расчёт» для активного режима в dashboard и API как `Manual Check`.
- [x] Согласовать внешние контрольные точки и ручной расчёт для полной валидации.

## P1. Фундамент под 3D

- [x] Описать общий словарь визуальных сигналов для 2D и 3D.
- [x] Подготовить схему `scene bindings` для будущей 3D-сцены.
- [x] Зарезервировать структуру каталогов для 3D-ассетов и scene-конфигураций.
- [x] Определить формат переключения `2D <-> 3D` без изменений расчетного слоя.
- [x] Добавить встроенную browser/WebGL-диагностику в Dash UI для локальной проверки возможностей клиента.
- [x] Проверить ограничения браузера и демо-ПК для будущего WebGL-режима.

## P1. Демонстрация и документы

- [x] Вынести ручные Playwright-скриншоты в отдельную структуру `artifacts/playwright/...`.
- [x] Подготовить demo-script на 5-7 минут.
- [x] Подготовить визуальный сценарий показа 2D-модели на защите.
- [x] Подготовить таблицу «технология -> зачем используется».
- [x] Подготовить таблицу «функция -> модуль проекта».
- [x] Подготовить список ограничений модели для ВКР.
- [x] Подготовить раздел о применении ИИ как вспомогательного инструмента.
- [x] Добавить `Demo Readiness` с launch checklist, runtime snapshot и browser preflight для офлайн-показа.
- [x] Добавить сборку `Demo Bundle` с zip/manifest, API-статусом и dashboard-кнопкой для упаковки проекта.

## P2. После MVP

- [x] Добавить архив сценариев и локальное сохранение прогонов в JSON.
- [x] Добавить экспорт `CSV/XLSX/PDF`.
- [x] Добавить журнал событий.
- [x] Добавить ручной и автоматический режимы.
- [x] Оценить целесообразность OpenModelica-адаптера.
- [x] Подготовить `Docker Compose` для воспроизводимого запуска.

## P3. Optional 3D-viewer после MVP

### Архитектура и зависимости

- [x] Зафиксировать решение по renderer-стеку для браузера: локально обслуживаемый `three.js` в `Dash assets` без обязательного `npm`/bundler-path.
- [x] Зафиксировать версию 3D-библиотеки и способ её обновления в `docs/10_sources.md` и проектной документации.
- [x] Сохранить принцип: 2D остаётся default/fallback renderer, а 3D подключается только как optional UI-адаптер поверх существующего `VisualizationSignalMap`.

### 3D-ассеты и scene metadata

- [x] Подготовить реальный `GLB/GLTF`-ассет установки и текстуры в `data/visualization/assets`.
- [x] Зафиксировать naming convention для узлов модели, чтобы имена в `GLB/GLTF` совпадали с `scene_node` из `data/visualization/scene3d.json`.
- [x] Расширить `data/visualization/scene3d.json`: добавить путь к основному ассету, camera presets, interactive targets, overlay anchors, flow/animation rules и performance-budget metadata.
- [x] Подготовить явную схему валидации `scene3d.json` и проверку наличия обязательных asset-файлов.
- [x] Зафиксировать правила экспорта модели из DCC-инструмента: единицы измерения, ориентация, допустимые текстуры и порядок обновления файла сцены.

### UI-интеграция и рендер

- [x] Добавить в dashboard переключатель `2D / 3D` с сохранением безопасного fallback на SVG.
- [x] Добавить отдельный контейнер 3D-сцены в `src/app/ui/layout.py` без смешивания 2D и 3D в одном renderer-слое.
- [x] Подключить новый clientside-asset, отвечающий за lifecycle 3D-viewer: инициализацию `scene/camera/renderer`, загрузку модели через `GLTFLoader`, orbit-навигацию через `OrbitControls` и teardown при размонтировании.
- [x] Ограничить рендер размерами контейнера и device-pixel budget, чтобы 3D-режим не деградировал UI на целевом demo-PC.

### Привязка сигналов и взаимодействие

- [x] Реализовать маппинг `VisualizationSignalMap -> scene nodes`: цвет, видимость, emissive/alarm highlight, подписи датчиков и интенсивность потока.
- [x] Добавить hover/click picking через `Raycaster` и вывод карточки элемента без обращения к расчётному ядру.
- [x] Определить, какие события сцены должны оставаться только clientside, а какие могут синхронизироваться с Dash-store.
- [x] Подготовить необязательный animation-layer для потоков и вращения вентилятора без зависимости от наличия анимаций внутри исходного `GLB`.

### Надёжность, производительность и приёмка

- [x] Добавить обработку состояний `loading / asset-error / unsupported-webgl / context-lost` с автоматическим возвратом в 2D.
- [x] Реализовать очистку ресурсов viewer при смене режима и обновлении сцены: `dispose()` для renderer/materials/geometries/textures и снятие обработчиков событий.
- [x] Добавить тесты и ручной checklist для 3D: валидация metadata, переключение 2D/3D, hover/click, fallback при недоступном WebGL, обновление сцены на базовых сценариях.
- [x] Обновить `browser_capability_profile.json` и Playwright-evidence, если для 3D потребуется новый verified browser/demo-PC envelope.
- [x] Подготовить отдельный demo-script для optional 3D-path, который не ломает основную 2D-защиту при отказе WebGL.

## P2.1. Пакет пользовательских улучшений (M10-M14)

Текущий статус на 2026-05-02: пакет доведен до release-ready уровня по
Phases A-E; Phase F синхронизирует документацию, acceptance matrix и regression
evidence.

### 1) Расширение симуляции (`Старт/Пауза/Сброс`, шаги времени)

- [x] Зафиксировать формат `simulation session` (id, шаг времени, состояние, история).
- [x] Добавить в сервисный слой операции `start`, `pause`, `reset`, `tick`.
- [x] Добавить API-контракты для управления жизненным циклом прогона.
- [x] Подключить UI-кнопки и защиту от некорректных переходов состояний.
- [x] Обновить тренды и KPI под пошаговое обновление.
- [x] Добавить unit/integration-тесты на управление сессией.
- [x] Добавить Simulation Session v2: `completed`, `playback_speed`, `max_ticks`, `horizon_reached`, `completed_at`, `last_command`.
- [x] Добавить runtime persistence активной сессии через `RuntimePathResolver`.

### 2) Отчетность по сценарию (`PDF/CSV`)

- [x] Выделить единый DTO/контракт отчета (метаданные прогона + таблицы + ключевые выводы).
- [x] Добавить CSV-выгрузку из этого контракта.
- [x] Добавить PDF-генерацию из того же контракта.
- [x] Сохранить manifest экспорта в `artifacts/exports/YYYY-MM-DD/`.
- [x] Добавить кнопки и статусы сборки отчета в dashboard.
- [x] Покрыть экспорт smoke-тестами и проверкой наличия файлов.
- [x] Добавить контракт `scenario-report.v2`, stable sections, chart specs и artifact checksums/file metadata.
- [x] Добавить preview/direct download/batch export flow.

### 3) Сравнение прогонов (`до/после`)

- [x] Добавить выбор пары прогонов из active run и `Scenario Archive`.
- [x] Реализовать вычисление дельт по KPI и диагностическим метрикам.
- [x] Добавить таблицу сравнения и отдельный график дельт.
- [x] Добавить валидацию совместимости сравниваемых прогонов.
- [x] Добавить экспорт сравнения в CSV/PDF.
- [x] Добавить тесты на граничные случаи сравнения.
- [x] Добавить runtime-снимки `До`/`После`, default selection, interpretation summary и top deltas.
- [x] Добавить `run-comparison.v2` CSV/PDF/manifest package.

### 4) Цветные статусы (`Норма/Риск/Авария`)

- [x] Зафиксировать единые пороги статусов в конфигурации.
- [x] Привязать статусы к KPI, мнемосхеме, трендам и alert-блокам.
- [x] Добавить легенду статусов на dashboard.
- [x] Добавить проверку согласованности статусов в view-model тестах.
- [x] Добавить отображение статусов в export-отчеты.
- [x] Сделать `StatusService` единственным источником пользовательских label/class/color/summary.
- [x] Сохранить `normal`, `warning`, `alarm` как технические enum/API значения без пользовательской подмены.

### 5) Пресеты сценариев (`Зима`, `Лето`, `Пик нагрузки`)

- [x] Проверить и обновить `data/scenarios/presets.json` под полный набор пресетов.
- [x] Добавить описание каждого пресета (назначение, ключевые параметры, ожидаемый эффект).
- [x] Добавить применение пресета в один клик на UI.
- [x] Добавить API-метку источника параметров (`manual`/`preset`).
- [x] Добавить тесты корректности диапазонов для всех пресетов.
- [x] Добавить контракт `scenario-preset.v2`: `schema_version`, `source`, `locked`, `created_at`, `updated_at`.
- [x] Добавить runtime user preset storage и CRUD/import/export без изменения `data/scenarios/presets.json`.

### 6) Release readiness

- [x] Обновить `docs/05_execution_phases.md`, `docs/06_todo.md`, `docs/10_sources.md`, `docs/15_demo_readiness.md`, README и `.omx` планы.
- [x] Обновить `.omx/plans/m10_m14_acceptance_matrix.md` конкретными evidence/test references.
- [x] Выполнить свежий `python -m pytest`.
- [x] Выполнить browser smoke dashboard на текущем окружении или явно зафиксировать ручной smoke-gap.
- [ ] Повторить browser walkthrough на целевом демо-ПК перед защитой.

## P2.2. Дистрибуция как приложения (Windows EXE + Android)

### 1) Windows EXE

- [x] Ввести единый writable runtime-dir (`%LOCALAPPDATA%/AhuSimulator`) для `exports`, `event-log`, `scenario-archive`, `demo-packages`.
- [x] Добавить launcher для desktop-режима: старт backend без `--reload`, авто-порт, открытие `/dashboard`.
- [x] Подготовить `PyInstaller` spec с включением `config/`, `data/`, `models/`, `images-of-models/` и нужных hidden imports.
- [x] Добавить скрипт сборки `deploy/build-windows-exe.ps1` и smoke-checklist запуска.
- [x] Подготовить installer-поставку (MSI/MSIX/Inno Setup) после стабилизации `onedir`.

### 2) Android mobile app

- [x] Создать `mobile/` shell-проект на Capacitor.
- [x] Настроить `capacitor.config` (`appId`, `appName`, Android platform, `server.url` на HTTPS endpoint).
- [x] Подготовить backend deployment для mobile-доступа (стабильный URL, HTTPS, CORS/security policy).
- [x] Провести mobile UX-адаптацию dashboard (touch-friendly controls, responsive layout, читаемость графиков).
- [x] Собрать debug APK и release APK/AAB, проверить на целевых устройствах.

Примечание по текущему окружению: debug APK, release APK (unsigned) и release AAB собраны локально; для проверки на целевых устройствах добавлены команды `npm run check:devices` и smoke-checklist в `mobile/README.md`, но на этой машине подключенные устройства не обнаружены.

### 3) Общая инфраструктура релиза

- [x] Добавить CI job для Windows build (PyInstaller).
- [x] Добавить CI job для Android build (Capacitor/Gradle).
- [x] Настроить versioning и подпись релизов (exe metadata + Android keystore).
- [x] Подготовить release-checklist для clean-install на Windows и Android.
- [x] Обновить docs по запуску/сборке/ограничениям desktop и mobile поставок.
